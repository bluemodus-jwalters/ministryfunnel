@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@model MinistryFunnel.FrontEnd.Models.DropDowns.MinistryOwnerCreateViewModel

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    @Html.LabelFor(m => m.MinistryOwnerId)
    @Html.DropDownListFor(t => t.MinistryOwnerId, Model.ministryOwners)
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
